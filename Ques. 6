Write a C program to print the following series:
BAZ, DBY, FCX, ...
---------------------------------------------------------------------

#include <stdio.h>
int main() {
    char first = 'B', second = 'A', third = 'Z';  // Initial characters

    for (int i = 0; i < 3; i++) {  // Print 3 terms
        printf("%c%c%c, ", first, second, third);
        first += 2;  // Increment by 2 for the first character
        second += 1; // Increment by 1 for the second character
        third -= 1;  // Decrement by 1 for the third character
    }

    return 0;
}



OUTPUT:-
BAZ, DBY, FCX,

